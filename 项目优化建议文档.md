# 项目优化建议文档

> CoolPig 个人网站 —— 全项目体检报告与优化路线图
> 体检日期：2026-07-30 ｜ 基于 Hexo 7.3 + Butterfly 5.5.4

---

## 目录

- [一、体检总结](#一体检总结)
- [二、有意保留的占位内容（不处理清单）](#二有意保留的占位内容不处理清单)
- [三、🔴 失效配置修复（高优先级）](#三-失效配置修复高优先级)
- [四、🟠 配置纠偏与冗余清理（中优先级）](#四-配置纠偏与冗余清理中优先级)
- [五、🟡 结构重构建议（改善可维护性）](#五-结构重构建议改善可维护性)
- [六、✨ 可增加的新功能](#六-可增加的新功能)
- [七、附录：项目现状档案](#七附录项目现状档案)

---

## 一、体检总结

对整个仓库（配置文件、CI/CD、内容结构、自定义代码、潜在风险五个维度）做了一次全面审查，总体结论：

**✅ 做得好的地方：**

| 项目 | 现状 |
|------|------|
| 仓库布局 | master 分支纯源码，生成产物独立发布到 gh-pages 分支，干净清晰 |
| 文章规范 | 10 篇文章 front matter 格式高度统一（title/excerpt/categories/tags/toc/abbrlink/date/updated 齐备） |
| SEO 基础 | sitemap（hexo-generator-sitemap）、RSS（hexo-feed）、本地搜索（searchdb）均已配置 |
| 敏感信息 | 全仓库无 token/密码残留，CI 仅使用内置 GITHUB_TOKEN |
| 自定义代码 | HLSL 高亮脚本、主题补丁系统、开发文档均维护良好 |
| 图片管理 | HLSL 笔记 30 张图已本地化，按章节规范命名 |

**⚠️ 需要处理的问题（按优先级）：**

| 优先级 | 数量 | 概述 |
|--------|------|------|
| 🔴 失效配置 | 2 项 | favicon 路径错误、dependabot 位置无效 |
| 🟠 配置纠偏 | 5 项 | deploy 遗留段、无用依赖、lockfile 策略、gitignore 残留等 |
| 🟡 结构重构 | 3 项 | inject 内联代码拆分、补丁本地化、permalink 策略 |
| ✨ 功能增强 | 5 项 | 懒加载、评论、404、统计、页脚 |

---

## 二、有意保留的占位内容（不处理清单）

以下内容经确认为**有意制作的占位**，等待后续填充，任何优化操作都不应删除或修改它们：

| 内容 | 位置 | 状态 |
|------|------|------|
| 作品集页面 | `hexo-site/source/portfolio/index.md` | 占位，待填充 |
| 论文页面 | `hexo-site/source/publications/index.md` | 占位，待填充 |
| 学术报告页面 | `hexo-site/source/talks/index.md` | 占位，待填充 |
| 教学页面 | `hexo-site/source/teaching/index.md` | 占位，待填充（内含一段 `Heading 2\n======` 坏语法，填充正式内容时顺手重写即可） |
| 示例 PDF | `hexo-site/source/files/paper1.pdf`、`paper3.pdf` | 占位，待替换为真实文件 |
| 占位图片 | `hexo-site/source/images/500x300.png` | 占位，待替换 |
| GitHub 社交链接 | `_config.butterfly.yml` L39 | 待后续补充真实链接（当前值 `Github \|\| '#24292e'\r` 缺 URL 且行尾有 `\r` 残留，补充时注意一并修正格式：`fab fa-github: https://github.com/CoolPig0720 \|\| Github \|\| '#24292e'`） |
| 被注释的导航菜单 | `_config.butterfly.yml` L30-L34（论文/报告/教学/作品集） | 页面填充后取消注释即可上线 |

> 💡 提示：这些占位页面虽然不在导航菜单中，但**仍会被 Hexo 生成**，直接访问 URL（如 `/publications/`）可以看到。如果介意，可以临时在页面 front matter 加 `sitemap: false` 让搜索引擎不收录，正式发布时再去掉。

---

## 三、🔴 失效配置修复（高优先级）

### 3.1 favicon 路径与实际文件不符 ✅ 已修复（2026-07-30）

- **位置**：`hexo-site/_config.butterfly.yml` L47
- **现状**：配置为 `favicon: /images/favicon.png`，但 `source/images/` 目录下实际文件是 `favicon.ico`
- **后果**：浏览器请求 `/images/favicon.png` 返回 404，网站标签页图标不显示
- **备注**：`开发文档.md` 第 253 行也自述了这个问题，属于已知但未修复
- **修复方案**（已采用方案 1）：
  1. 改配置：`favicon: /images/favicon.ico`（最简单，已同步更新开发文档）
  2. 把 ico 转成 png 放进 images 目录（如果想统一用 png）

### 3.2 dependabot 配置完全无效 ✅ 已修复（2026-07-30）

- **位置**：`hexo-site/.github/dependabot.yml`
- **问题 1**：GitHub 只识别**仓库根目录**下的 `.github/dependabot.yml`，放在 `hexo-site/.github/` 子目录中不会生效，等于从没开启过依赖更新检查
- **问题 2**：文件内 `directory: "/"` 指向仓库根，但 `package.json` 在 `/hexo-site` 下，即使移到正确位置也扫不到依赖
- **修复方案**（已全部完成）：
  1. 已新建仓库根目录 `.github/dependabot.yml`
  2. `directory` 已指向 `/hexo-site`
  3. 已改为 `weekly`，PR 上限降为 10；额外增加了 github-actions 生态的版本检查
  4. 已删除无效的 `hexo-site/.github/dependabot.yml`

---

## 四、🟠 配置纠偏与冗余清理（中优先级）

### 4.1 deploy 遗留配置与实际部署双轨 ✅ 已修复（2026-07-30）

- **位置**：`hexo-site/_config.yml` L137-L141
- **现状**：配置了 hexo-deployer-git 推送到 `main` 分支；但实际部署走 GitHub Actions（`deploy.yml`），产物发布到 `gh-pages` 分支
- **风险**：本地手滑执行 `hexo deploy` 会把生成产物推到 `main` 分支，与源码分支冲突，可能造成混乱
- **修复方案**（已完成）：已删除 `deploy:` 配置段（原位置改为部署说明注释）；`package.json` 中的 `"deploy": "hexo deploy"` script 已一并删除；开发文档“手动部署”章节已同步更新

### 4.2 无用依赖 ✅ 已修复（2026-07-30）

- **位置**：`hexo-site/package.json`
- **已移除**（npm uninstall，共减少 12 个包）：
  - `hexo-theme-landscape`：Hexo 默认主题，项目使用 butterfly，从未加载
  - `hexo-generator-i18n`：多语言生成器，站点为纯中文单语言，已确认 `_config.yml` 无 i18n 配置（`i18n_dir` 为 Hexo 内置默认项，与插件无关）
  - `hexo-deployer-git`：随 4.1 的 deploy 段一并移除
- **验证**：卸载后 `hexo generate` 成功，128 个文件正常生成

### 4.3 package-lock.json 被忽略 + CI 使用 npm install ✅ 已修复（2026-07-30）

- **位置**：`.gitignore` L13 + `.github/workflows/deploy.yml` L32
- **问题链条**：lockfile 不入库 → CI 每次 `npm install` 都按语义化版本拉取最新依赖 → 某个依赖发布不兼容新版时，**博客会在你什么都没改的情况下突然构建失败**（历史上 hexo 生态发生过多次）
- **修复方案**（已全部完成）：
  1. 已从 `.gitignore` 删除 `package-lock.json` 一行
  2. `hexo-site/package-lock.json` 已纳入版本控制
  3. `deploy.yml` 中 `npm install` 已改为 `npm ci`
  4. setup-node 步骤已加 npm 缓存（`cache: npm` + `cache-dependency-path`）
- **收益**：构建 100% 可复现；配合修好的 dependabot，依赖升级变为受控的 PR 流程

### 4.4 .gitignore 残留 Jekyll 时代规则 ✅ 已顺手修复（2026-07-30，随 4.3 一并清理，仅保留 node/hexo/IDE/系统规则）

- **位置**：`.gitignore` L1-L21
- **现状**：`_site/`、`Gemfile.lock`、`/vendor/`、`.bundle/`、`.sass-cache/` 是前身 Academic Pages（Jekyll）项目的规则，当前 Hexo 项目完全用不到
- **修复方案**：清理为只保留 node/hexo/IDE/系统相关规则，让文件意图清晰
- **优先级**：低，纯洁癖项，顺手做即可

### 4.5 _config.yml 中的非标准配置块 ✅ 已修复（2026-07-30，已删除 butterfly.live2d 块）

- **位置**：`hexo-site/_config.yml` L121-L124
- **现状**：存在 `butterfly.live2d` 配置块，这不是 Hexo 标准配置项，Butterfly 主题读取的是 `_config.butterfly.yml`（其中已有 `live2d.enable: false`），此块无任何效果
- **修复方案**：删除即可

---

## 五、🟡 结构重构建议（改善可维护性）

### 5.1 inject 内联 CSS/JS 拆分为独立文件 ⭐ 最推荐 ✅ 已完成（2026-07-30）

- **位置**：`hexo-site/_config.butterfly.yml` L374-L810（约 437 行）
- **现状**：约 **324 行 CSS + 108 行 JS** 以 YAML 多行字符串形式内联在主题配置中，涵盖十余个功能区块：
  - CSS：网站标题放大、导航样式、侧边栏样式、表格居中、正文排版、归档页分类分组、阅读进度条、三栏布局、右侧 TOC 容器、最新文章标题截断等
  - JS：TOC 右移、rightside 按钮滚动显隐、归档页分类展开/收起、阅读进度条
- **痛点**：
  - YAML 里没有 CSS/JS 语法高亮和格式化，改动易出错
  - 缩进敏感，一个空格错位整个配置解析失败
  - 无法用编辑器的代码折叠/跳转，437 行翻找困难
  - 大量 `!important` 强制覆盖，样式优先级混乱
- **重构结果**（行为完全不变，已机械化脚本迁移确保一字不差）：
  1. 已新建 `hexo-site/source/css/custom.css`（323 行），`<style>` 块内容原样迁入
  2. 已新建 `hexo-site/source/js/custom.js`（108 行），`<script>` 块内容原样迁入
  3. `inject` 段已精简为外部引用（CSS 走 head 避免样式闪烁，JS 走 bottom）：
     ```yaml
     inject:
       enable: true
       head:
         - <link rel="stylesheet" href="/css/custom.css">
       bottom:
         - <script src="/js/custom.js"></script>
     ```
  4. 主题配置文件从 823 行瘦身到 392 行
- **验证结果**：`hexo clean + generate` 后 8 项检查全部通过（首页正确引用两个外部文件、无内联残留、public 产物存在且内容完整、归档页补丁正常）
- **收益**：主题配置文件从 823 行瘦身到约 390 行；后续所有样式修改都在独立文件中进行，有语法高亮、可版本对比
- **后续可选**：拆分后可逐步给 CSS 分区加注释目录、清理不再需要的 `!important`

### 5.2 主题补丁本地生效脚本 ✅ 已完成（2026-07-30）

- **现状**：`themes/_patches/` 下 4 个补丁文件仅在 CI（`deploy.yml` L34-L40）通过 `cp` 覆盖到 node_modules；**本地 `hexo s` 预览时补丁不生效**，看到的归档页分组、TOC 层级等和线上不一致
- **重构结果**：
  1. 已新建 `hexo-site/apply-patches.js`（Node 脚本，跨平台）：自动递归遍历 `themes/_patches/`，逐一复制到 `node_modules/hexo-theme-butterfly/` 对应路径
  2. `package.json` 已增加 `patch` 和 `postinstall` scripts
  3. `deploy.yml` 补丁步骤已简化为 `npm run patch`（4 条 cp 命令 → 1 行）
  4. 本地验证：`npm run patch` 成功应用 4 个补丁文件，归档页分组补丁在本地构建中生效
  5. 开发文档“主题补丁系统”章节已同步更新（新增补丁不再需要改 workflow）
- **收益**：本地/CI 行为统一；新增补丁文件时不再需要同步修改 workflow（脚本自动遍历目录）；`npm install` 后补丁自动就位
- **附带风险提示**：`main.js`/`utils.js` 是**整文件覆盖**而非最小 diff，未来升级 Butterfly 主题（如 5.5.4 → 5.6.x）时，官方对这两个文件的更新会被补丁覆盖掉，需要手动重新合并。升级主题时务必先 diff 官方新版文件与补丁文件

### 5.3 permalink 策略（可选，谨慎）

- **现状**：安装了 hexo-abbrlink、每篇文章都有 abbrlink 字段，但 `permalink: :year/:month/:day/:title/` 并未使用它。中文标题在 URL 中会被编码成超长字符串（如 `/2026/07/30/Unity-%E5%AD%A6%E4%B9%A0%E7%AC%94%E8%AE%B0.../`）
- **可选方案**：改为 `permalink: posts/:abbrlink/`，得到 `posts/34095/` 形式的永久短链
- **⚠️ 代价**：**所有已发布文章的 URL 全部改变**，已分享出去的旧链接、搜索引擎已收录的页面全部 404
- **建议**：如果站点尚在早期、外部引用少，越早改代价越小；如果决定不改，可以考虑卸载 hexo-abbrlink 并删除文章中的 abbrlink 字段（但注意：**保留现状也无害**，abbrlink 字段还起着防止插件回写清空文章的作用，建议维持现状直到做出决定）

---

## 六、✨ 可增加的新功能

### 6.1 图片懒加载（推荐，零成本高收益） ✅ 已完成（2026-07-30）

- **位置**：`_config.butterfly.yml`，已改为 `lazyload.enable: true`
- **动机**：HLSL 笔记单篇最多 30 张图，原来打开文章会一次性请求全部图片
- **已采用配置**：`field: post`（仅文章页）+ `blur: true`（加载前模糊占位）
- **验证结果**：生成的文章页中 15 张内容图片全部改写为 `data-lazy-src`，界面图片（Logo/头像）不受影响
- **⚠️ 踩坑记录**：`field: post` 模式通过 `after_post_render` 过滤器改写文章，受 db.json 缓存影响——**开关懒加载后必须 `hexo clean` 再构建**，否则旧缓存里的文章不会被改写

### 6.2 评论系统（推荐 giscus）

- **现状**：`comments.use` 为空，所有文章无评论区
- **推荐方案**：**giscus** —— 基于 GitHub Discussions，与 GitHub Pages 天然匹配：
  - 免费、无需自建后端、数据存在自己仓库的 Discussions 里
  - 访客用 GitHub 账号登录评论（技术博客受众高度重合）
- **接入步骤**：
  1. 仓库 Settings → 开启 Discussions 功能
  2. 安装 giscus GitHub App 并授权仓库
  3. 在 [giscus.app](https://giscus.app) 生成配置参数（repo-id、category-id）
  4. `_config.butterfly.yml` 填入：
     ```yaml
     comments:
       use: Giscus
     giscus:
       repo: CoolPig0720/CoolPig0720.github.io
       repo_id: <生成的 repo-id>
       category_id: <生成的 category-id>
       theme_light: light
       theme_dark: dark
     ```
- **备选**：Twikoo（需部署云函数）、Waline（需部署服务端）——都比 giscus 重，不推荐首选

### 6.3 404 页面 ✅ 已完成（2026-07-30）

- **原现状**：配置文件是精简版，完全缺失 `error_404` 配置段，访问不存在的页面显示 GitHub 默认 404
- **已采用配置**（`_config.butterfly.yml`）：
  ```yaml
  error_404:
    enable: true
    subtitle: "页面走丢了，去首页看看吧"
    background: /img/404.jpg   # 主题自带插图；本站 default_top_img 为空，不指定会渲染空图
  ```
- **验证结果**：构建产物 130 → 131 个文件，新增 `404.html` 位于站点根目录，标题/副标题/背景图均正确渲染，GitHub Pages 会自动接管根目录的 404.html

### 6.4 访问统计

按投入从轻到重两档：

| 方案 | 配置 | 效果 |
|------|------|------|
| **busuanzi**（轻量） | `_config.butterfly.yml` 中 `busuanzi.enable: true` | 站点 PV/UV、单页阅读次数，即开即用，无需注册 |
| **Google Analytics**（完整） | 注册 GA4 拿到 measurement ID，填入 `analytics.google` | 完整访客画像、来源分析、实时在线 |

两者可共存：busuanzi 负责页面上展示数字，GA 负责后台分析。

### 6.5 页脚 footer ✅ 已完成（2026-07-30）

- **原现状**：配置中仅有 `footer_img: false`，缺失 footer 主体配置段（一直在用主题内置默认值渲染，不可控）
- **已采用配置**（`_config.butterfly.yml`）：
  ```yaml
  footer:
    nav:                 # 预留：占位页面完善后可加页脚导航
    owner:
      enable: true
      since: 2025        # 建站年份，自动显示为 © 2025 - 当前年 By CoolPig
    copyright:
      enable: true
      version: true      # 显示 Hexo / Butterfly 版本
    custom_text: 记录游戏开发与图形学习之路 🎮
  ```
- **验证结果**：首页页脚正确渲染版权区间、自定义文案、框架信息三部分
- **可选进阶**（未做）：在 custom.js 中加"网站运行天数"计时器

### 6.6 图片点击放大（medium_zoom） ✅ 已完成（2026-07-30）

- **动机**：HLSL 笔记函数曲线图、节点图看细节只能靠浏览器缩放
- **已采用配置**（`_config.butterfly.yml`）：`lightbox: medium_zoom`（Butterfly 5.x 用统一的 lightbox 键，可选 fancybox / medium_zoom）
- **验证结果**：文章页与首页均已注入 medium-zoom 脚本，与懒加载兼容（主题内部处理了两者配合）
- **未采纳的候选**（用户决策）：图片题注 photofigcaption、文章置顶 sticky、RSS 订阅、评论与访问统计（明确不要）

### 6.7 相关文章推荐（related_post） ✅ 已完成（2026-07-30）

- **发现**：主题默认 `related_post.enable: true`，精简配置没有该键时一直在默默生效；本次显式声明到配置并把 limit 从 6 调到 4（全站文章少，6 篇容易凑数）
- **机制**：按 tag 重合度加权推荐，重合越多排越前；无任何 tag 重合的文章（如"好用的网站"）不显示推荐区块，不硬凑
- **验证结果**：HLSL 系列互推、Unity 篇推 GameEvents（双标签权重最高）、面经篇推同类面试文 + Unity 笔记，limit 4 生效

---

## 七、附录：项目现状档案

### 7.1 依赖清单（package.json，18 项）

| 依赖 | 版本 | 状态 |
|------|------|------|
| hexo | ^7.0.0 | ✅ 核心 |
| hexo-theme-butterfly | ^5.5.4 | ✅ 主题 |
| hexo-abbrlink | ^2.2.1 | ⚠️ 已装但 permalink 未使用（字段兼作防回写用途） |
| hexo-deployer-git | ^4.0.0 | ⚠️ 配合遗留 deploy 段，建议随其一起移除 |
| hexo-feed | ^1.1.2 | ✅ RSS |
| hexo-generator-archive / category / index / tag | ^2/^2/^3/^2 | ✅ 基础生成器 |
| hexo-generator-searchdb | ^1.5.0 | ✅ 本地搜索 |
| hexo-generator-sitemap | ^3.0.1 | ✅ SEO |
| hexo-generator-i18n | ^0.0.10 | ❓ 疑似未使用，建议确认后移除 |
| hexo-math | ^5.0.0 | ✅ MathJax |
| hexo-renderer-ejs / marked / stylus | ^2/^6/^3 | ✅ 渲染器 |
| hexo-server | ^3.0.0 | ✅ 本地预览 |
| hexo-theme-landscape | ^1.0.0 | ❌ 未使用，建议移除 |
| hexo-wordcount | ^6.0.1 | ✅ 字数统计 |

### 7.2 CI/CD 流程（.github/workflows/deploy.yml）

```
push master/main（或手动触发）
  → checkout（fetch-depth 0）
  → Node 20
  → cd hexo-site && npm install          ← 建议改 npm ci + 缓存
  → cp 4 个补丁覆盖 node_modules 主题     ← 建议改 npm run patch
  → npm run build（hexo generate）
  → peaceiris/actions-gh-pages@v3 发布 public/ 到 gh-pages（force_orphan）
```

### 7.3 自定义代码资产

| 资产 | 作用 |
|------|------|
| `hexo-site/scripts/hlsl-highlight.js` | 注册 HLSL 高亮语言 + 注入 hexo-util 白名单（182 行） |
| `themes/_patches/layout/archive.pug` | 归档页按分类分组可折叠 |
| `themes/_patches/layout/includes/widget/card_post_toc.pug` | TOC 组件层级/展开控制 |
| `themes/_patches/source/js/main.js` | 主 JS 整文件替换（归档页交互、导航修复等） |
| `themes/_patches/source/js/utils.js` | 工具 JS 整文件替换 |
| `_config.butterfly.yml` inject 段 | ~324 行 CSS + ~108 行 JS（建议拆分，见 5.1） |

### 7.4 内容清单（10 篇文章）

| 文章 | 分类 | 标签 |
|------|------|------|
| 好用的网站 | 技术文档 | 网站、编程 |
| 网站优化记录 | 技术文档 | — |
| 面试题整理 | 面试问题 | 面试 |
| 游戏功能测试面试题（疯狂游戏） | 面试问题 | 游戏测试、面试、功能测试 |
| 游戏功能测试面试题（欢乐互娱） | 面试问题 | 游戏测试、面试 |
| HLSL 学习笔记（三）：时间动画 | 技术文档 | HLSL |
| HLSL 学习笔记（四）：常用数学函数 | 技术文档 | HLSL |
| HLSL 学习笔记（五）：距离场与基础形状 | 技术文档 | HLSL |
| Unity 学习笔记（一）：GameEvents 事件中心 | 技术文档 | Unity、C# |
| Unity 学习笔记（二）：泛型对象池 | 技术文档 | Unity、C# |

### 7.5 建议执行顺序

```
第一批（10 分钟）：3.1 favicon + 3.2 dependabot
第二批（20 分钟）：4.1 deploy 段 + 4.2 无用依赖 + 4.3 lockfile/npm ci + 4.5 非标准配置块
第三批（1 小时）： 5.1 inject 拆分 + 5.2 补丁本地化脚本
第四批（按需）：  6.1 懒加载 → 6.3 404 页面 → 6.5 footer → 6.4 统计 → 6.2 giscus 评论
随缘处理：       4.4 gitignore 清理 / 5.3 permalink 决策
```

---

*本文档由项目全面体检生成，执行任何一项前建议先本地 `hexo clean && hexo s` 验证，再推送触发 CI 部署。*
